# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SpiceTracker2' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SpiceTracker2
  pod 'Firebase/Database'
  pod 'Firebase/Core'

  target 'SpiceTracker2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SpiceTracker2UITests' do
    # Pods for testing
  end

end
